COMPONENT=MainConfig
CFLAGS += -I$(TOSDIR)/lib/printf
#CFLAGS += -DPRINTF_BUFFER_SIZE=250
#FLAGS+=-DCC2420_DEF_CHANNEL=11 # from 11 to 26
CFLAGS+=-DCC2420_DEF_RFPOWER=31 # power from 1 to 31
CFLAGS+=-DTOSH_DATA_LENGTH=65 
include $(MAKERULES)
